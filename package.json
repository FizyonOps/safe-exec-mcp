{
  "name": "@fizyonops/safe-exec-mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Safe command execution MCP server (whitelist + timeouts + no-shell)",
  "main": "dist/server.js",
  "bin": {
    "safe-exec-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "start": "node dist/server.js",
    "dev": "vitest --watch",
    "test": "vitest run --reporter=basic"
  },
  "keywords": ["mcp", "server", "safe", "exec", "shell", "qwen", "claude", "gemini"],
  "author": "FizyonOps",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
